import React from 'react';
import CustomCamera from '../../components/CustomCamera';

const CameraPage = ({ route }: { route: any }) => {
	return <CustomCamera route={route} />;
};

export default CameraPage;
