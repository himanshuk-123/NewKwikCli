# Native

Abstractions over platform capabilities (camera, filesystem, location). Components and services consume these wrappers instead of direct native/SDK calls. Implementations can swap per platform.
